package com.ocp.cert.chapter3.list_set_map_queue;

public class Commons {

	/*
	 * TreeMap and TreeSet use compareTo() and always sorted
	 */

	/*
	 * Hashtable, HashSet and HashMap use hashCode()
	 */

	/*
	 * TreeMap can't contain null keys + TreeSet can't contain null values +
	 * ArrayDeque can't contain null values due to poll() uses null as special value
	 * + Hashtable no null keys and values
	 */
}
